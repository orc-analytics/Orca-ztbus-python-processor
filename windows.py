from typing import TypedDict
from orca_python import WindowType

EveryMinute = WindowType(name="EveryMinute", version="1.0.0", description="Triggered every minute") 
AppliedBreaks = WindowType(name="AppliedBreaks", version="1.0.0", description="When the brakes are applied") 

